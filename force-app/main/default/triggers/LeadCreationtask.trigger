trigger LeadCreationtask on Lead (after update,after insert) {

   /* if(trigger.isupdate && trigger.isafter){
        ConvertingFlowstoTriggerhandlerclass.whenleadCrtedCretetask(Trigger.new,trigger.oldmap);
    } */
    
   /*  if(trigger.isinsert && trigger.isafter){
     TriggerAssignHandlerClasses.createduplicateLead(trigger.new);
     } */
    
}