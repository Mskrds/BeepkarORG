trigger OpportunityLineItemTrigger on OpportunityLineItem (After insert,After delete,before insert) {

   /* if(trigger.isafter && trigger.isInsert){
        TriggerAssignHandlerClasses.updateQuantityOnAccount(trigger.new); 
    } */
    
  /*  if(trigger.isdelete && trigger.isafter){
    TriggerAssignHandlerClasses.whenOLIDelete2Opportunity(trigger.old);
    } */
    
    
   /*19  if(trigger.isafter && trigger.isInsert){
        TriggerAssignHandlerClasses.createquotewhenOLICreated(trigger.new);
    
     } */
   /*   if(trigger.isafter && trigger.isInsert){
        TriggerAssignHandlerClasses.createAssetwhenOLICreated(trigger.new);
     } */
     /*  if(trigger.isafter && trigger.isInsert){
    Opprtunitytriggerhelper.createAssetwhenOLICreated(trigger.new); 
} */
  /* if(trigger.isafter && trigger.isInsert){
    Practicemapclass.CreateAsset(trigger.new); 
} */
    
    
  /* if(trigger.isafter && trigger.isInsert){
   TriggerAssignHandlerClasses.UpdateTotalSalesQuantity(trigger.new);
      
    }  */
  /*    if(trigger.isbefore && trigger.isInsert){
   TriggerAssignHandlerClasses.checkOppF1equalstoOLIF3(trigger.new);
      
    } */
   /*   10.   if(trigger.isafter && trigger.isInsert){
   TriggerAssignHandlerClasses.sendEmailToOppAccEmail(trigger.new);
      
    } */
    
}