trigger ContactObjTriggers on Contact (before insert,After update,After insert) {
  /*  if(trigger.isupdate && trigger.isafter){
        ConvertingFlowstoTriggerhandlerclass.PopulateEmailonAcc(Trigger.new,Trigger.oldmap);
    }
    
    if(trigger.isInsert && trigger.isafter){
        ConvertingFlowstoTriggerhandlerclass.SendEmailToConEmail(Trigger.new);
    }  
    if(trigger.isinsert && trigger.isbefore){
    TriggerAssignHandlerClasses.preventduplicaterecord(trigger.new);
    
} */
  /*     if(trigger.isInsert && trigger.isafter){
        TriggerAssignHandlerClasses.whencontactcreatesendemailtemplate(Trigger.new);
} */
}