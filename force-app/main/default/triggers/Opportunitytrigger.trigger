trigger Opportunitytrigger on Opportunity (before insert, after update, after insert) {
    
    
    /*if(trigger.isafter && trigger.isupdate){
        Opprtunitytriggerhelper.creditupdatemethod(trigger.oldmap, trigger.newmap);
    } */
  /*  if(trigger.isafter && trigger.isinsert){
        TriggerAssignHandlerClasses.opportunityinsertion(trigger.new);
} */
    
    
 /*   if(trigger.isafter && trigger.isupdate){
        TriggerAssignHandlerClasses.UpdateamounttoAcc(trigger.new); 
}*/
 /*   if(trigger.isafter && trigger.isupdate){
        TriggerAssignHandlerClasses.updatemultipickvalues(trigger.new,trigger.oldmap); 
} */
 /*   if(trigger.isafter && trigger.isupdate){
        TriggerAssignHandlerClasses.updateAccclientconwithOpp(trigger.new,trigger.oldmap); 
}  */
  /*  if(trigger.isafter && (trigger.isupdate || trigger.isinsert)){
        TriggerAssignHandlerClasses.WhenoppMinandMaxchangedupdateAccMinMaxDate(trigger.new);
    
    
    } */
    
}